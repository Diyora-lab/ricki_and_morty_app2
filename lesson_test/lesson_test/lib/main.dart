import 'package:flutter/material.dart';

void main() {
  runApp(const TestLesson());
}

class TestLesson extends StatelessWidget {
  const TestLesson({super.key});

  @override
  Widget build(BuildContext context) {
    return const MaterialApp();
  }
}
